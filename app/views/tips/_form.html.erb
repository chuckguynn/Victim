<h4>Share a Tip and Help Solve a Crime</h4>

<%= form_for(@tip) do |f| %>
  <% if @tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tip.errors.count, "error") %> prohibited this tip from being saved:</h2>

      <ul>
      <% @tip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <label>(All tips are anonymous, and not seen by the public!)</label>
<%= f.hidden_field :post_id %>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Share' %>
  </p>

<% end %>

